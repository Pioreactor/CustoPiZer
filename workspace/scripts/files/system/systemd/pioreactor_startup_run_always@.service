[Unit]
Description=Start up %I on boot. Used for run-always jobs.
Wants=network-online.target
After=network-online.target firstboot.service

[Service]
User=pioreactor
ExecStart=pio run-always %I
Restart=no
Environment="PATH=/home/pioreactor/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin"

[Install]
WantedBy=multi-user.target
